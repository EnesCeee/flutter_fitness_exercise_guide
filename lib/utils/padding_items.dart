import 'package:flutter/material.dart';

class ProjectPadding {
  static const pagePaddingVertical = EdgeInsets.symmetric(vertical: 30);
  static const pagePaddingHorizontal = EdgeInsets.symmetric(horizontal: 20);
  static const kUltraAllPadding = EdgeInsets.all(20);
  static const kHighAllPadding = EdgeInsets.all(15);
  static const kRegularAllPadding = EdgeInsets.all(10);
  static const kDefaultAllPadding = EdgeInsets.all(8);
  static const kHighBottomPadding = EdgeInsets.only(bottom: 15);
  static const kRegularBottomPadding = EdgeInsets.only(bottom: 10);
  static const kHighRightPadding = EdgeInsets.only(right: 15);
  static const kRightPadding = EdgeInsets.only(right: 10);
  static const kTopPadding = EdgeInsets.only(top: 20);
}
